按你现在这套 RouterRAG 流程，模型是：

- `Embedding`：`text-embedding-v4`
- `实体抽取`：`qwen-plus`
- `关系抽取`：`qwen-plus`

对应配置在 [llm.yaml](/f:/opinion-system/opinion-system/backend/configs/llm.yaml) 里：
- `embedding_llm.model`
- `router_vec_llm.model`

补一句：检索后的“LLM总结”也是 `qwen-plus`（`router_retrieve_llm.model`）。